---
import "./close-button.scss";

interface Props {
  id?: string;
  label?: string;
  class?: string;
}

const { id, label = "Cerrar", class: className } = Astro.props;
---

<button
  type="button"
  class:list={["btn-close", className]}
  id={id}
  aria-label={label}
>
  {label}
</button>
