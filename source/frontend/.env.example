# Sanity (lectura desde el front). Opcional si ejecutas desde la raíz del monorepo (npm run dev:web).
# Si ejecutas solo el frontend (npm run dev dentro de frontend/), define aquí o en frontend/.env
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
